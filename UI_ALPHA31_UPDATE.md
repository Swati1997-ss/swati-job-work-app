# Swati App — UI Alpha 31

## Fixes included
- Removed the old small duplicate sidebar expand/collapse mark near the menu labels.
- Kept only the new rounded chevron button on the right side.
- Updated app splash/install branding to **Swati Mini Oil Mill**.
- Bumped app asset versions to reduce stale-cache issues in installed PWA builds.

## Notes
If the device still shows old splash branding after deploying this build, reinstalling the PWA once may be needed because Android sometimes keeps the old cached app label.
